# Frontend

Vite + React + TypeScript starter for the Product Analytics Dashboard.

Quick start:

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Set the backend URL with `VITE_API_URL` in `frontend/.env`.
